# Ansible Galaxy local cache/lock (roles/collections are reinstalled via requirements.yml)
.ansible/

# macOS
.DS_Store

# Ansible retry files
*.retry

# Ansible Vault password file (never commit — see inventory/vault/argocd_dex.yml)
.vault_pass.txt
