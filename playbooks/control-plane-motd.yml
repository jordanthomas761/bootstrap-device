- name: Control-Plane MOTD
  hosts: control_plane

  tasks:
    - name: Copy MOTD
      become: true
      become_method: ansible.builtin.sudo
      vars:
        source_path: control-plane-motd
      ansible.builtin.copy:
        src: "{{ source_path }}"
        dest: /etc/update-motd.d/51-control-plane-motd
        mode: +x
    - name: Check if MOTD exists
      ansible.builtin.stat:
        path: /etc/motd
      register: motd_stat
    - name: Check MOTD
      ansible.builtin.slurp:
        src: /etc/motd
      register: motd_value
      when: motd_stat.stat.exists
    - name: Erase Default MOTD
      become: true
      become_method: ansible.builtin.sudo
      ansible.builtin.copy:
        content: ""
        dest: /etc/motd
        mode: "0644"
      when: motd_stat.stat.exists and (motd_value.content | b64decode) != ""
    - name: Check env file groups
      ansible.builtin.shell: grep -c 'PATH' /etc/environment || true
      changed_when: false
      register: env_file
    - name: Append Groups
      become: true
      become_method: ansible.builtin.sudo
      ansible.builtin.lineinfile:
        path: /etc/environment
        line: PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/local/games:/usr/games"
      when: env_file.stdout == "0"
